{
  "$id": "https://example.com/product.schema.json",
  "title": "SpawnNPC",
  "description": "Request to spawn an NPC in the simulator.",
  "type": "object",
  "properties": {
    "id": {
      "description": "The ID of the NPC to be spawned.",
      "type": "string"
    },
    "type": {
      "description": "The type of the NPC to be spawned.",
      "type": "string"
    },
    "configuration": {
      "description": "The configuration name to be applied to the object to be spawned.",
      "type": "string"
    },
    "location": {
      "description": "The location of the object to be spawned.",
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 3,
      "maxItems": 3
    },
    "rotation": {
      "description": "The rotation of the object to be spawned.",
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 3,
      "maxItems": 3
    },
    "pathXCoordinates": {
      "description": "The X coordinates of the NPC's path to be spawned.",
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 6,
      "maxItems": 1000
    },
    "pathYCoordinates": {
      "description": "The Y coordinates of the NPC's path to be spawned.",
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 6,
      "maxItems": 1000
    },
    "pathZCoordinates": {
      "description": "The Z coordinates of the NPC's path to be spawned.",
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 6,
      "maxItems": 1000
    },
    "landmark": {
      "description": "When present, the location is ignored and the object to be spawned will be placed at the landmark specified by this field.",
      "type": "string"
    },
    "tags": {
      "description": "Tags or metadata to be associated with the object to be spawned",
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 0,
      "maxItems": 20
    },
    "payloads": {
      "description": "JSON payloads specific to the object to be spawned.",
      "type": "array",
      "items": {
        "type": "object"
      },
      "minItems": 0,
      "maxItems": 20
    }
  },
  "required": [ "id", "type", "pathXCoordinates", "pathYCoordinates"],
  "oneOf": [
    {"required": ["location"]},
    {"required": ["landmark"]}
  ]
}
